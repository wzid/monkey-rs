let x = 9876543210;
let y = 1234567890;

let abs = fn(x) {
    if x < 0 {
        return -x;
    }
    return x;
};

let gcd = fn(a, b) {
    if b == 0 {
        return abs(a);
    } else {
        return gcd(b, a % b);
    }
};

gcd(x, y);